# V2 Plus CN 1.4

This version fixes PNG export by cloning the current slide into a clean 1080 x 1350 export frame before running html2canvas.

It also keeps the larger preview mode from version 1.3.

Recommended flow:

1. Load index.html
2. Apply 1080 x 1350 size
3. Renumber pages
4. Export current PNG

Use the local ZIP version if editor.html has not been updated online.
